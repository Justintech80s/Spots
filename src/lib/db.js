const url=import.meta.env.VITE_SUPABASE_URL||'https://ulooczkmzxtaoehizwoj.supabase.co';const key=import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY;export const configured=Boolean(url&&key);
async function request(path,options={}){if(!configured)throw new Error('SPOTS database key is not configured');const token=localStorage.getItem('spots_access_token');const res=await fetch(url+'/rest/v1/'+path,{...options,headers:{apikey:key,Authorization:'Bearer '+(token||key),'Content-Type':'application/json','Accept-Profile':'spots','Content-Profile':'spots',...(options.headers||{})}});if(!res.ok)throw new Error('SPOTS database request failed: '+res.status);return res.status===204?null:res.json()}
export const db={properties:()=>request('properties?select=*&order=base_value.asc'),listings:()=>request('listings?select=*&status=eq.open&order=created_at.desc'),ownership:id=>request('ownership?property_id=eq.'+id+'&select=*'),credentials:id=>request('credentials?owner_id=eq.'+encodeURIComponent(id)+'&public=eq.true&verification_status=eq.verified&select=*')};